export interface Usuario {
    nombre: string;
    apellidos: string;
    telefono: string;
  }

  export interface Impresora {
      nombre: string;
      codigo: string | number;
  }

  export interface Departamento {
      nombre: string;
      codigo: string;
  }

